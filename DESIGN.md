# パチスロ実戦データ解析ツール 設計書

## 概要
ホールの実戦データ（`hall_data.db`, SQLite）をブラウザ上で読み込み、機種別・設置台数グループ別に
「累積差枚の傾向と翌日勝率の関係」を多角的に分析するクライアントサイド専用SPA。
サーバーを持たず、sql.js（WASM版SQLite）でブラウザ内にDBをロードし、全集計をSQLで行う。

## 技術構成
- **sql.js 1.14.2**：SQLiteエンジンをWASMでブラウザ実行。`SqlDriver`が唯一の直接操作窓口。
- **Chart.js 4**：折れ線グラフ描画（ランキング・閾値・月次トレンドの3タブ）。
- **素のJS + HTML + CSS**：フレームワーク非依存。単一`app.js`にロジックを集約。

## データ層（SqlDriver）
DBファイルロード後、以下のインデックスを毎回作成し直す（メモリ上DBのため永続化されない）。

- `idx_hall_data_date`, `idx_hall_data_name_date`, `idx_hall_data_no_date`, `idx_hall_data_date_name`

`hall_data`テーブルの想定列は `date, machine_no, machine_name, diff` で、これを土台に全分析が構築される。

## 共通フィルタとベースCTE（buildBaseCte）
全分析タブが共有する中核ロジック。以下の段階でCTEを構築する。

1. **digit_days**：日付末尾フィルタ（例：末尾3,7のみ）と期間フィルタを適用した対象日集合。
2. **machine_counts**：`(date, machine_name)`単位の実測稼働台数。他のフィルタと無関係に全データから算出（設置台数グループ判定の基準をぶれさせないため）。
3. **dated**：`machine_counts`と結合し、設置台数グループラベル（`count_group`）と`day_num`（Julian日数）を付与。
4. **rolling**：`machine_no`ごとに`RANGE BETWEEN N PRECEDING AND 1 PRECEDING`のウィンドウ関数で、対象日を含まない直前N日間の累積差枚（`cum_diff`）とルックバック実日数を計算。カレンダー日数ベースのため欠損日は遡って埋めない。
5. **target**：ルックバック実日数が1以上の行のみ、かつ対象日集合に含まれる行に絞る。
6. **ranked**：`(date, machine_name)`単位で`cum_diff`昇順・降順のRANK（`rank_worst`, `rank_best`）と当日の設置台数（`group_size`）を付与。
7. **joined**：勝ち判定（`diff > 0`）を`is_win`として付与した最終形。以降の全分析はこの`joined`をFROM対象にする。

機種フィルタ・設置台数グループフィルタは、このCTE自体には反映せず、各分析クエリのSELECT側でWHERE条件として個別に適用する「独立軸」として扱う（`resolveActiveAxes`）。両方選択された場合はクロス集計ではなく、系列を並べて重ね描きする方式。

## 統計計算モジュール
ロジックを機能ごとに小さなモジュールへ分離している。

- **WilsonScore**：勝率の95%信頼区間（正規近似より小標本に強い）。
- **ZScore**：勝率・平均差枚・差枚中央値のスケールを揃えるための標準化。総合スコア計算の前処理。

いずれも他の分析ロジックと独立しており、単体で再利用可能な形にしている。

## 分析機能

### 1. ランキング別勝率
同機種内で当日の累積差枚順位（`rank_worst`）ごとに、勝率・平均差枚・差枚中央値・信頼区間・総合スコアを集計。中央値と分散はSQL側でウィンドウ関数（`ROW_NUMBER`/`COUNT`）を使い算出し、信頼区間はJS側で計算する。表の並び順（順位順／信頼度順／総合スコア順）は直前のSQL結果をキャッシュ（`lastRankingResult`）して再ソートのみで切り替え、DBへの再クエリを避ける。グラフのx軸は常に順位順で固定。

### 2. 閾値別勝率
累積差枚を固定範囲（-30,000〜30,000）内で指定刻み幅のビンに分割し、ビンごとの勝率を集計する従来方式。

### 3. 月次トレンド
「特定順位（ワースト1〜10位、複数選択可）」または「閾値条件（以上/以下）」のいずれかを軸に、年月別の勝率推移を集計。順位モードでは系列（機種／設置台数グループ）×順位の組み合わせごとに、色相＝系列・明度＝順位で塗り分けて描画する。

### 4. 複合条件スコア探索
機種×累積差枚条件×ルックバック日数の総当たり探索。累積差枚条件は3パターン（累積差枚順位のみ／しきい値のみ／複合〈順位×しきい値〉）から切り替え可能。順位を用いるパターン（順位のみ・複合）では、ワースト順位（`rank_worst`）とTOP順位（`rank_best`）の集計を`UNION ALL`で1つの結果セットにまとめ、順位列に💀（ワースト）・👑（TOP）のアイコンを付与して1テーブル内で横断比較できるようにする（例：「💀1」「👑3」）。

- 設置台数から「その日において意味のある順位の上限」（`target_n`：10台以上→5位、6〜9台→3位、2〜5台→1位、1台→判定不能）を算出し、ユーザーが選択した順位のうち`target_n`以内の行だけを対象順位として個別集計する。この上限は💀・👑どちらの方向にも同様に適用する。
- ルックバック日数は複数選択可能で、候補ごとにCTEを作り直して個別クエリし、結果をJS側で合流させる。
- 結果は「対象期間の最終日に設置されている機種」と「されていない機種」の2テーブルに分割表示。
- 最低件数（n）フィルタで少数サンプルの偶然の好結果を事前に除外し、既定の並び順は信頼区間下限順（総合スコア順は多重比較の影響を受けやすいため補助的な選択肢）。
- 探索パターン切り替え時は結果をクリアし、再実行を促す。
- 設置台数が少なく累積差枚が同点の場合、`RANK()`の仕様上ワースト1位とTOP1位が同一行になることがあり、統合後は💀1・👑1として別カウントされるが数値上は同じ行が2つ表示される点に留意する。

## UI構成
- **タブ切り替え**：4分析を`.tab-panel`で切り替え表示。
- **フィルタサイドパネル**：`position: fixed`のオーバーレイ式で、通常レイアウトに影響を与えずに開閉。
- **結果テーブルの折りたたみ**：`<details>`要素で初期非表示にし、グラフを主役に据える構成。
- **計算中インジケーター**：`runWithIndicator`でボタンを無効化＋テキスト変更し、1フレーム後に処理実行。

## 主な設計判断・注意点
- 信頼区間・総合スコアはいずれも「参考指標」であり、統計的に厳密な境界値の代わりに運用上の目安（`MIN_RELIABLE_N`など）を用いている。
- 複合条件スコア探索は組み合わせ数が多いほど多重比較問題の影響を受けやすいため、最低件数フィルタと信頼区間下限順を主軸に据える設計とした。
- ルックバック日数はSQLのウィンドウ幅に直接埋め込まれるため、複数日数を同時に見るには都度CTEを再構築する必要があり、その分クエリ回数が増える。
- 機種フィルタ・設置台数グループフィルタは独立軸として重ね描きするのみで、両者のクロス集計（例：特定機種×特定台数グループ）は現状未対応。
